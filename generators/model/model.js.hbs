const mongoose = require('mongoose')

const {{name}}Schema = new mongoose.Schema({
  title: String,
  value: Number,
})

const {{properCase name}}Model = mongoose.model('{{properCase name}}Model', {{name}}Schema, '{{pluralize name}}')

module.exports = {
  {{properCase name}}Model,
}
